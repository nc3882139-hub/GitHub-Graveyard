# Server Configuration
PORT=3000

# GitHub Configuration
# Get your personal access token from https://github.com/settings/tokens
# Scopes needed: repo (full control of private repositories)
GITHUB_TOKEN=ghp_your_github_personal_access_token_here

# Webhook Configuration
# Generate a random secret to secure your webhook
# This must match the secret configured in your GitHub repository settings
WEBHOOK_SECRET=your_random_webhook_secret_here

# Optional API bearer token. When set, API requests must send Authorization: Bearer <token>.
API_AUTH_TOKEN=

# Optional comma-separated origins for local cross-origin development.
ALLOWED_ORIGINS=http://localhost:5500,http://localhost:3000

# Optional server logging level: DEBUG, INFO, WARN, or ERROR.
LOG_LEVEL=INFO

# Environment
NODE_ENV=development
